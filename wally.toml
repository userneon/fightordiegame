[package]
name = "nomadbros/fightordie"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

# Add shared dependencies here as the project grows, e.g.:
# [dependencies]
# Signal = "sleitnick/signal@1.5.0"
[dependencies]
